#include <iostream>
using namespace std;
int main(){
	int cur = 0;
	int score[5]= {0, };
	int d[4];
	for(int i = 0 ; i < 5; i++){
		cin >> d[0] >> d[1] >> d[2] >> d[3];
		if (i != cur){
			score[i] = 
		}
		cur++;
	}
return 0;
}
